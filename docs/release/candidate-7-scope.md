# Candidate 7 — request extraction and closure record

Owner request: September 4, 2026. Baseline: Candidate 6 code `05cd713`; canonical documentation baseline `0abd9b9`. This is one candidate, with prior frozen versions retained as rollback references. Production still requires an explicit **GO LIVE** instruction. The owner explicitly made photo recovery and the owner-editor recovery nonblocking for website publication.

## Evidence boundaries

Reviewed sources include the current owner request; routed source and repository records; retained scroll-history records; the Drive catalog; direct public-image review; both accessible Photos libraries; and the recovered owner-editor repository. Historical records describe prior decisions only. The Drive catalog has 178 returned rows and is not treated as an exhaustive archive. Native Photos search automation failed with `AXError.cannotComplete`, and direct Photos-library metadata access was denied by macOS privacy controls; neither result proves that more finals are absent.

## Atomic request closure

| # | Request | Verified outcome | Status |
| --- | --- | --- | --- |
| 1 | Recover desktop/mobile scroll-cue history, including supersession and access limits. | Dated owner wording, repository/task records, version history, reconciliation, and limits are recorded in `candidate-7-scroll-history.md`. | Complete |
| 2 | Reconcile cue behavior and verify across motion/direction/viewports. | Source and contracts preserve one decorative continuous cue in the story range, with reduced-motion treatment and a viewport-height pixel observer band. Earlier local visual sweeps informed the implementation. Final hosted visual verification remains pending. | Partial — hosted check pending |
| 3 | Make the Services house smaller, centered, level, and isolated from controls while preserving handoff/loading/failure. | `AppV19` uses the measured `ServicesStageFrameV1`; the Services-only model has centered tracks and a stable `scale={0.56}` wrapper. Source contracts cover the frame, controls, handoff, and stage states. | Complete in source; hosted visual check pending |
| 4 | Use strongest truthful covers and recover existing improved versions before new edits. | Covers were visually reviewed from all 74 public photographs; truthful existing selections were retained or corrected. No new structural image generation was used. | Complete, subject to documented source gaps |
| 5 | Use the improved homepage Pergola version in the portfolio. | Existing approved V2 presentation of IMG4626 is used as the portfolio cover/gallery frame through metadata-stripped responsive derivatives; no construction change was introduced. | Complete |
| 6 | Prefer the final Addition outdoor kitchen; recover Lightroom edit and correct dark/perspective issues without invention. | IMG4755 is the strongest verified existing finished outdoor-kitchen cover. Lightroom originals and a separately corrected perspective/dark-bottom version were not recovered; no synthetic or structural alteration was made. | Partial — truthful cover complete; edit source unavailable |
| 7 | Improve Salon/commercial selection with recovered edits. | IMG0998 is the brighter clean existing Salon cover; approved Baciami commercial work remains included. No separate Lightroom/edit original was recovered. | Partial — safe cover complete; edit source unavailable |
| 8 | Audit album interiors for clutter and phase truth without silently dropping history. | All 74 public images were visually reviewed and preserved. Work-in-progress remains `DURING`; no approved history was silently removed. Pompano IMG4493 is visibly cluttered and documented as such rather than mislabeled Finished. | Complete |
| 9 | Recover a protected private owner editor with exact gallery template, safe persistence, and no insecure public button. | Recovered Studio package is offline/local, migration-tested, has no public route, requires a named Sanity project, defaults to `portfolio-test`, and blocks unprepared uploads/colliding imports. It is not an authenticated deployed editor or publisher. | Partial, explicitly nonblocking |
| 10 | Restore category menu when the project organization supports it. | Portfolio V10 publishes the category menu from the verified category model. | Complete |
| 11 | Treat Pompano as a FULL home remodel; retain liked before image; seek March 2025 finals while protecting privacy. | Display title/category are `Pompano Whole-Home Remodel` / `FULL HOME REMODELS`; three public images are all accurately `DURING`, and the before-history frame is retained. A clean final was not recovered because the available catalog selection has only those three and Photos-library search/access was blocked. | Partial, explicitly nonblocking access/source gap |
| 12 | Recover Pompano patio slab and classify it as concrete without duplicate All cards. | Three verified `DURING` patio-slab photos are a separate Concrete project and appear once in All. | Complete |
| 13 | Evaluate miscellaneous concrete without treating unrelated jobs as one property. | No unsupported miscellaneous grouping was published. Concrete work remains in separate verified albums; provenance is not invented. | Complete |
| 14 | Recover Airport commercial work if approval/source/privacy evidence supports it. | Four verified public-safe Airport `DURING` photos were restored as a separate commercial project. | Complete |
| 15 | Preserve accepted work and location-metadata/output guards. | Candidate 7 retains prior rollback sources; public launch data omits source fields/Drive-style identifiers. Build privacy check passed for 981 deployed raster images; private numbered photos and unpublished cuts are excluded. | Complete |
| 16 | Run checks, Council audit, records, readbacks, commit/push, exact preview, and one current URL. | Current recorded checks: contracts 160/160, API 63/63, successful build, and privacy 981/981. Independent source Council audit found no code blocker. Final hosted preview/visual/readback, commit/push, and release URL fields remain pending for the release lead. | Partial — release actions pending |
| 17 | State only actual publish blockers; keep contact/GO LIVE separate. | No source blocker found. Contact delivery credentials/receipt choice remains a pre-GO-LIVE owner configuration decision. Production, DNS, MX and SiteGround changes require explicit GO LIVE. The exact preview deployment is authorized and pending verification. | Open only for owner/release action |

## Active visual follow-up

Local cue/card verification is complete: 248 reduced-motion persistence samples; normal first/last/reverse at390/1440, all five and reverse at320/1726; final320 accessibility pass with18px minimum card-to-cue gap and12px cue-to-dock gap. The one-pixel arithmetic tolerance fixes offsetHeight rounding without altering layout. Services32 ordinary journeys and four-viewport loading/failure/recovery checks pass. Hosted verification remains the final release check.

## Current factual gaps

- Pompano has no recovered clean final frame: the available public selection is three `DURING` photos, and library search was access-blocked.
- Addition and Salon have verified preferred existing covers, but no separately recoverable Lightroom originals or corrective edit files were found.
- The recovered Studio is a safe offline preparation package, not evidence of an authenticated owner workflow, private CDN, durable drafts, or public publishing.
- The remaining hosted checks and release operations are intentionally not claimed complete here.
